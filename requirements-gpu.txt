# GPU / CUDA extras — install after requirements.txt
#
# Example (CUDA 12.4):
#   pip install -r requirements.txt
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
#
# flash-attn (optional, for some decoder embedders):
#   pip install flash-attn --no-build-isolation
#
# After first install on the internet machine, freeze for bit-identical envs:
#   pip freeze > requirements-lock.txt
