# Pinned for reproducible MTEB scores across machines.
# Install torch separately for your CUDA version (see requirements-gpu.txt).

mteb==2.18.0
sentence-transformers==5.6.0
transformers==4.57.6
datasets==3.6.0
huggingface_hub==0.34.4
accelerate==1.6.0
einops==0.8.1

# Optional faster HF downloads on prefetch machine:
# pip install "mteb[xet]"
